<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Categoría</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-100 min-h-screen flex items-center justify-center p-4">

  <%- include('../partials/header') %>

  <div class="bg-gray-800 border border-gray-700 rounded-lg shadow-lg w-full max-w-md p-6">
    <h2 class="text-2xl font-semibold text-red-400 mb-4">
      Editar categoría #<%= categoria.id_categoria %>
    </h2>
    <form action="/categorias/editar/<%= categoria.id_categoria %>" method="POST" class="space-y-4">
      <input type="text" name="nombre" value="<%= categoria.nombre %>" required autofocus
             class="w-full bg-gray-700 text-gray-100 border border-gray-600 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-red-500"
      />
      <button type="submit"
              class="w-full bg-red-600 hover:bg-red-700 text-white font-semibold px-4 py-2 rounded transition">
        Guardar cambios
      </button>
    </form>
    <p class="mt-4 text-center">
      <a href="/categorias" class="text-red-400 hover:text-red-500 underline">
        ← Volver al listado
      </a>
    </p>
  </div>

</body>
</html>
